package com.cgi.pages;

import org.openqa.selenium.WebDriver;

/**
 * Handle all menu and common elements in the page
 */
public class MainPage {
private WebDriver driver;
	
	public MainPage(WebDriver driver)
	{
		this.driver=driver;
	}
}
